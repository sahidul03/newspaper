<section class="content">
  <!-- Main row -->
  <div class="row">
    <!-- Left col -->
    <section class="col-lg-12 connectedSortable">
      <div class="box box-info">
        <div class="box-header">
          <i class="fa fa-check"></i>
          <h3 class="box-title">Second Last Right Side Ad</h3>
          <!-- tools box -->

        </div>
        <%= form_for @slr_ad do |f| %>

            <div class="box-body">
              <%= f.text_field :link, :class=>'form-control', :placeholder=>'Ad link....' %>
              <div class="form-group pad">
                <div class="btn btn-success btn-file">
                  <i class="fa fa-paperclip"></i> Ad Image
                  <%= f.file_field :ad_picture, :class=>'' %>
                </div>
                <p class="help-block">Size(160,unlimited)</p>
              </div>
            </div>
            <div class="box-footer clearfix">
              <%= f.submit :class=>'pull-right btn btn-default' %>
              <!--<button class="pull-right btn btn-default" id="sendEmail">Create <i class="fa fa-check"></i></button>-->
            </div>
        <% end %>
      </div>
      <!-- Box (with bar chart) -->
      <div class="box box-info">
        <div class="box-header">
          <i class="fa fa-check"></i>
          <h3 class="box-title">Second Last Right Side Ad</h3>
          <!-- tools box -->

        </div>
        <!-- Box (with bar chart) -->
        <!-- quick email widget -->


        <div class="table-responsive">
          <!-- .table - Uses sparkline charts-->
          <% if @second_last_right_ads.any? %>
              <table class="table table-striped">
                <tr>
                  <th class="col-md-4">Ad</th>
                  <th class="col-md-4">Link</th>
                  <th class="col-md-2">Date</th>
                  <th class="text-right col-md-2">Delete</th>
                </tr>
                <% @second_last_right_ads.each do |slr_ad| %>
                    <tr>
                      <td>
                        <%= image_tag(slr_ad.ad_picture, :class => 'ad_image_show') %>
                      </td>

                      <td><%= slr_ad.link %></td>
                      <td><%= slr_ad.created_at.strftime('%d %B, %y') %></td>
                      <td class="text-right">

                        <div class="btn-group">
                          <%= link_to second_last_right_side_ad_path(slr_ad), :class=>'btn btn-info', method: :delete,data: { confirm: 'Are you sure?' } do %>
                              <i class='fa fa-align-center fa-crop'></i> delete
                          <% end %>
                        </div>

                      </td>
                    </tr>
                <% end %>
              </table><!-- /.table -->
          <% end %>

        </div>
      </div>
    </section><!-- /.Left col -->
    <!-- right col (We are only adding the ID to make the widgets sortable)-->

  </div><!-- /.row (main row) -->

</section><!-- /.content -->
